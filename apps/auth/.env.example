# =============================================================================
# Orbit Auth Server Configuration
# =============================================================================

# ----------------------------------------
# Database Configuration
# ----------------------------------------
# PostgreSQL connection string (supports Neon, Supabase, or any PostgreSQL)
DATABASE_URL=postgresql://user:password@hostname/database?sslmode=require

# ----------------------------------------
# Better Auth Configuration
# ----------------------------------------
# The URL where this auth server is accessible
# This should be the public URL in production
BETTER_AUTH_URL=http://localhost:5000

# Secret key for signing sessions and tokens (minimum 32 characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-min-32-chars

# ----------------------------------------
# OAuth Client Origins
# ----------------------------------------
# Comma-separated list of trusted client origins
# Add ALL your OAuth client application URLs here
#
# Example for multiple clients:
#   TRUSTED_ORIGINS=http://localhost:3000,https://app.mysite.com,https://admin.mysite.com
#
# In production:
#   TRUSTED_ORIGINS=https://client1.example.com,https://client2.example.com
#
# Note: The BETTER_AUTH_URL is automatically added to trusted origins
TRUSTED_ORIGINS=http://localhost:3000

# ----------------------------------------
# Production Checklist
# ----------------------------------------
# Before deploying to production:
# [ ] Set BETTER_AUTH_URL to your production domain (e.g., https://auth.example.com)
# [ ] Generate a strong BETTER_AUTH_SECRET with: openssl rand -base64 32
# [ ] Add all client application URLs to TRUSTED_ORIGINS
# [ ] Use HTTPS for all URLs
# [ ] Set up proper database backups
